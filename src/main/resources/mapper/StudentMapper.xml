<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.job.mapper.StudentMapper">

    <resultMap id="BaseResultMap" type="com.job.entities.Student">
            <id property="studentId" column="student_id" jdbcType="INTEGER"/>
            <id property="amountId" column="amount_id" jdbcType="INTEGER"/>
            <id property="majorId" column="major_id" jdbcType="INTEGER"/>
            <id property="resumerId" column="resumer_id" jdbcType="INTEGER"/>
            <result property="gender" column="gender" jdbcType="INTEGER"/>
            <result property="contact" column="contact" jdbcType="VARCHAR"/>
            <result property="address" column="address" jdbcType="VARCHAR"/>
            <result property="graduateFlag" column="graduate_flag" jdbcType="INTEGER"/>
            <result property="direction" column="direction" jdbcType="INTEGER"/>
            <result property="avatar" column="avatar" jdbcType="VARCHAR"/>
            <result property="date" column="date" jdbcType="TIMESTAMP"/>
            <result property="degree" column="degree" jdbcType="INTEGER"/>
            <result property="salary" column="salary" jdbcType="INTEGER"/>
            <result property="industry" column="industry" jdbcType="VARCHAR"/>
            <result property="post" column="post" jdbcType="VARCHAR"/>
            <result property="remark" column="remark" jdbcType="VARCHAR"/>
            <result property="birthday" column="birthday" jdbcType="TIMESTAMP"/>
            <result property="deleteFlag" column="delete_flag" jdbcType="INTEGER"/>
    </resultMap>

    <select id="selectStudentList">
        SELECT
            s.student_id,
            s.`name`,
            s.avatar,
            m.`name` AS major_name,
            a.amount_id,
            a.username,
            s.gender,
            s.remark,
            s.contact,
            s.address,
            s.birthday,
            s.graduate_flag,
            s.date,
            s.degree,
            s.industry,
            s.post,
            s.salary
        FROM student s
        LEFT JOIN amount a ON a.amount_id = s.amount_id
        LEFT JOIN resume r ON s.student_id = r.resumer_id
        LEFT JOIN major m ON m.major_id = s.major_id
    </select>
</mapper>
